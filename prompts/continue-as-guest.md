Requirements:
- Implement `continue as guest` feature both client and server side
- Utilize PWA features to create one Guest per device
- Prefer to not use `isGuest` flag or similar in database schema, it dangerous
- Guest can do anything like normal user
